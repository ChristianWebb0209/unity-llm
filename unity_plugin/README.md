# Unity Plugin Workspace (Generated)

This directory is created by `setup_unity_prd_workspace.ps1`.

Next actions:
1. Implement a Unity Editor plugin (main UX) that streams to the FastAPI backend.
2. Implement Unity-side tool execution (apply file/scene/UI changes).
3. Port the Composer v3 tool schema to a Unity tool executor contract.
